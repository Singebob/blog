---
import Center from "../../layouts/Center.astro"
import { getCollection } from 'astro:content';

const rules = await getCollection('rules');
---

<Center title="Règle">
    {rules.map(rule => (
      <div>
        <a href={`/rules/${rule.id}`} class="flex items-center gap-2">
          <h4>{rule.data.pageTitle}</h4>
          <span class={`text-xs px-2 py-1 rounded-full ${rule.data.type === 'Code' ? 'bg-blue-100 text-blue-800 dark:bg-blue-800 dark:text-blue-100' : 'bg-green-100 text-green-800 dark:bg-green-800 dark:text-green-100'}`}>
            {rule.data.type}
          </span>
        </a>
      </div>
      <hr />
    ))}
</Center>
